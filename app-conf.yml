
# Java context path
server.servlet.context-path: /java

# URL to node app
app.node.url: http://node-app:8081/

spring:
  jpa:
    properties.hibernate:
      temp.use_jdbc_metadata_defaults: false

  datasource:
    url: jdbc:postgresql://postgres-srv:5432/demodb
    username: ${DB_USER}
    password: ${DB_PASSWORD}
